import { styled } from "@mui/material/styles";
import { BaseCircularProgress } from "./BaseCircularProgress";

export const BaseCircularProgress = styled(BaseCircularProgress)(
  ({ theme }) => ({
    // Styles
  })
);
